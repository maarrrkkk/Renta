<?xml version="1.0" encoding="utf-8"?>
<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background with blur effect -->
    <item>
        <bitmap android:src="@drawable/login_bg" /> <!-- Replace with your background image -->
        <color android:color="#80000000" /> <!-- Adjust the color and opacity as needed -->
    </item>
    <!-- CardView content (adjust the margin to your liking) -->
    <item android:top="8dp" android:right="8dp" android:bottom="8dp" android:left="8dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="8dp" />
        </shape>
    </item>
</layer-list>
